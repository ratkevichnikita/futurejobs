import AuthListener from "@/src/app/AuthListener";

const MainPage = () => {
  return (
    <main className="max-w-[1280px] max-h-[723px] h-[100vh] w-full relative mx-auto overflow-hidden">
      <AuthListener />
    </main>
  );
}

export default MainPage
