import AuthListener from "@/src/app/AuthListener";

const MainPage = () => {
  return (
    <main className="h-[40.5vw] w-[71.5vw] relative mx-auto overflow-hidden">
      <AuthListener />
    </main>
  );
}

export default MainPage
